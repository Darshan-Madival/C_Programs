#include<stdio.h>
#include<conio.h>
#include<string.h>
#include<stdlib.h>
#define mul(a,b) (a)*(b)//function like macros 
int main()
{
    int a = 2, b = 3;
    printf("%d", mul(a, b));
    return 0;
}